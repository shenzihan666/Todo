<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only: allow HTTP to your PC’s LAN IP (see local.properties local.server.host). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
