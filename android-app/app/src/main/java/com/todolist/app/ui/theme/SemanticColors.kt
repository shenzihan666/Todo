package com.todolist.app.ui.theme

import androidx.compose.ui.graphics.Color

/** Income / expense accents (aligned with common finance UI). */
object FinanceColors {
    val Income = Color(0xFF34C759)
    val Expense = Color(0xFFFF453A)
}

/** Proposed-action row accents in confirmation sheet. */
object ActionColors {
    val Create = Color(0xFF0A84FF)
    val Update = Color(0xFFFF9F0A)
    val Delete = Color(0xFFFF453A)
    val Confirm = Color(0xFF34C759)
}
